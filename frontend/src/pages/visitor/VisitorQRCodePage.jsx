import React from "react";
import { Download, QrCode } from "lucide-react";
export function VisitorQRCodePage({ appState, setAppState }) { const pass = appState.passes[0]; const save = () => setAppState(s=>({...s, passes:s.passes.map(p=>p.id===pass.id?{...p,savedToPhone:true}:p)})); return <section className="mobileModule"><div className="qrCard"><QrCode size={132}/><strong>{pass?.ownerName || "No QR yet"}</strong><span>{pass?.id} | {pass?.type} | Hash {pass?.hash}</span><em>{pass?.type === "Group" ? `${pass.participantCount} participants | one-time group QR` : "Personal QR | Active"}</em></div><button className="primaryButton" onClick={save}><Download size={18}/> Save QR to Phone</button><div className="notice">Show this QR code at the guardhouse scanner.</div></section>; }
