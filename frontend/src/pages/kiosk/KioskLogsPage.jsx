import React from "react";
import { AlertTriangle, Database } from "lucide-react";
export function KioskLogsPage({ appState }) { return <section className="panel"><div className="sectionHead"><div><p className="eyebrow">Entrance Operation</p><h2>Logs</h2><p>Invalid QR alerts, duplicate check-in alerts, Face ID review, offline sync, scanner heartbeat and checkout logs.</p></div><Database size={30}/></div><div className="split"><div className="subPanel"><h3>Operational Alerts</h3>{appState.alerts.map(a=><div className="feedItem" key={a.id}><strong><AlertTriangle size={15}/> {a.message}</strong><em>{a.time} | {a.severity} | {a.status}</em></div>)}</div><div className="subPanel"><h3>System Logs</h3>{appState.logs.map((l,i)=><div className="logLine" key={i}>{l}</div>)}</div></div><h3>Offline Queue Records</h3><table className="dataTable"><thead><tr><th>ID</th><th>Type</th><th>Name</th><th>Status</th></tr></thead><tbody>{appState.offlineQueue.map(q=><tr key={q.id}><td>{q.id}</td><td>{q.entryType}</td><td>{q.name}</td><td>{q.status}</td></tr>)}</tbody></table></section>; }
