import React from "react";
import { Activity, AlertTriangle, MessageSquare, TrendingUp, Users } from "lucide-react";
import { MetricCard } from "../shared/MetricCard";
import { liveHeadCount } from "../../data/appState";
export function StaffOverviewPage({ appState, setPage }) { return <section className="panel"><div className="sectionHead"><div><h2>Dashboard Overview</h2><p>Staff landing page summarising live operations, alerts, feedback and AI prediction.</p></div></div><div className="metricRow"><MetricCard icon={Users} label="Visitors today" value={appState.visits.reduce((s,v)=>s+Number(v.count||1),0)}/><MetricCard icon={Activity} label="Currently inside" value={liveHeadCount(appState.visits)}/><MetricCard icon={AlertTriangle} label="Active alerts" value={appState.alerts.filter(a=>a.status==="Active").length} tone="amber"/><MetricCard icon={MessageSquare} label="New feedback" value={appState.feedback.filter(f=>f.status==="New").length}/></div><div className="dashboardGrid"><button className="overviewCard" onClick={()=>setPage("live")}><Users/><strong>Live Monitoring</strong><span>View current occupancy and activity feed.</span></button><button className="overviewCard" onClick={()=>setPage("forecast")}><TrendingUp/><strong>Forecasting</strong><span>Next peak expected this weekend.</span></button><button className="overviewCard" onClick={()=>setPage("feedback")}><MessageSquare/><strong>Feedback</strong><span>Review visitor sentiment and responses.</span></button></div></section>; }
