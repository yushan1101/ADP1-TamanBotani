import React, { useState } from "react";
import { Camera, CheckCircle2, ScanFace } from "lucide-react";
export function VisitorFaceIdPage({ appState, setAppState }) { const [done,setDone]=useState(false); const enroll=()=>{const p=appState.passes[0]; setAppState(s=>({...s, visitors:s.visitors.map(v=>v.id===p.ownerId?{...v,faceId:true}:v)})); setDone(true)}; return <section className="mobileModule">{done ? <div className="successBox"><CheckCircle2 size={54}/><strong>Face ID enrolled</strong><span>Entrance camera can now detect your Face ID automatically.</span></div> : <><div className="faceNotice"><ScanFace size={52}/><strong>Add Face ID</strong><span>Face ID is optional. The system stores a mathematical face embedding, not your photo. It is used only for check-in verification and expires after 12 months.</span></div><label className="checkLine"><input type="checkbox"/> I agree to biometric enrolment.</label><button className="primaryButton" onClick={enroll}><Camera size={18}/> Open Camera & Capture</button></>}</section>; }
